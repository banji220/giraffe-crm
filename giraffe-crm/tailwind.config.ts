// Tailwind v4 uses CSS-based configuration via @theme in globals.css.
// This file is retained only to suppress any tooling that expects its
// presence — it has no effect on the build.
export default {}
